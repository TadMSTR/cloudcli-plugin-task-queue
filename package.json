{
  "name": "cloudcli-plugin-task-queue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx tsc && npx esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --sourcemap"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.4",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
